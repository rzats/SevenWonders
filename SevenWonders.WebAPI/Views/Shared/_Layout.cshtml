<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewBag.Title</title>
    <script src="https://ajax.aspnetcdn.com/ajax/jQuery/jquery-3.2.1.min.js"></script>

    @Styles.Render("~/Content/css")
    @Scripts.Render("~/bundles/modernizr")


</head>
<body id="page-top">
    <nav id="mainNav" class="navbar navbar-default navbar-fixed-top">
        <div id="mainNav1" class="container-fluid">
            <div class="navbar-header">
                <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
                    <span class="sr-only">Toggle navigation</span> Menu <i class="fa fa-bars"></i>
                </button>
                <a class="navbar-brand page-scroll" href="#page-top">7 wonders</a>
            </div>

            <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
                <ul class="nav navbar-nav navbar-left">
                    <li>@Html.ActionLink("Home", "Index", "Home")</li>
                    <li>@Html.ActionLink("Hot tours", "Index", "HotTours")</li>
                    <li>@Html.ActionLink("Contact", "Index", "Contact")</li>
                    @if (User.IsInRole("admin"))
                    {
                        <li>@Html.ActionLink("Countries", "Index", "Countries")</li>
                        <li>@Html.ActionLink("Managers", "SearchManagers", "Admin")</li>
                        <li>@Html.ActionLink("Customers", "SearchCustomers", "Admin")</li>

                        <li class="dropdown ">
                            <a class="dropdown-toggle " data-toggle="dropdown" href="#">Statistics<span class="caret"></span></a>
                            <ul class="dropdown-menu">
                                <li>@Html.ActionLink("Countries statistic", "CountriesStatistic", "Reports")</li>
                                <li>@Html.ActionLink("Managers statistic", "ManagersStatistic", "Reports")</li>

                            </ul>
                        </li>
                    }
                    else if (User.IsInRole("manager"))
                    {
                        <li>@Html.ActionLink("Countries", "Index", "Countries")</li>
                        <li>@Html.ActionLink("Cities", "Index", "Cities")</li>
                        <li>@Html.ActionLink("Hotels", "Index", "Hotels")</li>
                        <li>@Html.ActionLink("Airports", "Index", "Airports")</li>
                        <li>@Html.ActionLink("Flights", "Index", "Flights")</li>
                        <li>@Html.ActionLink("Tours", "Index", "Tours")</li>
                    }
                </ul>
            </div>
        </div>
    </nav>
    <div>
        @RenderBody()
    </div>
    @Scripts.Render("~/bundles/jquery")
    @Scripts.Render("~/bundles/Statistic")
    @Scripts.Render("~/bundles/jqueryui")
    @Scripts.Render("~/bundles/bootstrap")
    @RenderSection("scripts", required: false)


    @*<script>
            var selector = '.nav li';

            $(selector).on('click', function () {
                $(selector).removeClass('active');
                $(this).addClass('active');
            });
        </script>*@
    <script>
    $(document).ready(function () {
        $('.dropdown-toggle').dropdown();
    });


        $(window).on('resize', function () {

            var width = $(window).width();

            if (width < 975) {
                $('header').height('1200px');
            }
            else
            {
                $('header').height('840px');
            }

            if ($('#mainNav').height() == 100) {
                document.body.style.paddingTop = '100px'
            }
            else if ($('#mainNav').height() == 150) {
                document.body.style.paddingTop = '150px'
            }
            else if ($('#mainNav').height() == 200) {
                document.body.style.paddingTop = '200px'
            }
            else
            {
                document.body.style.paddingTop = '50px'
            }
    }).trigger('resize');
   //

    </script>

</body>
</html>
